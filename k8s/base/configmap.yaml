apiVersion: v1
kind: ConfigMap
metadata:
  name: trailblazer-ai-config
  labels:
    app.kubernetes.io/name: trailblazer-ai
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # Add non-sensitive configuration here
  # NEXT_PUBLIC_APP_URL: "https://trailblazer.example.com"
