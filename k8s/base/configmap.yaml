apiVersion: v1
kind: ConfigMap
metadata:
  name: trailblazer-ai-config
  labels:
    app.kubernetes.io/name: trailblazer-ai
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # Pay-i proxy for transparent AI call instrumentation
  PAYI_PROXY_URL: "http://payi-proxy:8000"
