apiVersion: v1
kind: Secret
metadata:
  name: trailblazer-ai-secrets
  labels:
    app.kubernetes.io/name: trailblazer-ai
type: Opaque
stringData:
  # Replace with actual values or use external secrets management
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  MAPBOX_ACCESS_TOKEN: ""
  DATABASE_URL: ""
  NEXTAUTH_SECRET: ""

  # Provider-specific encryption keys for API key storage (32 bytes base64 each)
  # Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
  ANTHROPIC_ENCRYPTED_KEY: ""
  OPENAI_ENCRYPTED_KEY: ""
  GOOGLE_ENCRYPTED_KEY: ""
  XAI_ENCRYPTED_KEY: ""
  BEDROCK_ENCRYPTED_KEY: ""
